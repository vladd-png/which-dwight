class Card {
  constructor(card) {
    this.idNumber = card.idNumber;
    this.imgSource = card.imgSource;
    this.flippedCardOver = card.flippedCardOver;
    // this.matched = false;
    // this.value = null;
  }

}
