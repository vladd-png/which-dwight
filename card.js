class Card {
  constructor(card) {
    this.idNumber = card.idNumber;
    this.imgSource = card.imgSource;
    // this.matched = false;
    // this.value = null;
  }


  match() {
    // this.matched = true;
  }
}
