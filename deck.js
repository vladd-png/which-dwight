class Deck {
  constructor(card) {
    this.cards = card;
    this.matchedCards = null;
    this.selectedCards = null;
    this.matches = matches;

    this.value = card.value;
  }

  shuffle() {
  }

  checkSelectedCards() {
  }

  moveToMatched() {
  }
}
